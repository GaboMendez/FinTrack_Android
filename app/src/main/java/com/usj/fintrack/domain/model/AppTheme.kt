package com.usj.fintrack.domain.model

/**
 * User-selected theme preference stored in DataStore.
 * [SYSTEM] follows the device light/dark setting.
 */
enum class AppTheme {
    LIGHT,
    DARK,
    SYSTEM
}
